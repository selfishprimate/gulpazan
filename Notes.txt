
Important Notes

1. For Unix based systems like MacOs there can be some security issues when trying to install the dependencies. If so, please use the command below:

$ sudo npm install --unsafe-perm=true --allow-root


2. You can remove the node_modules folder by using the command below:

$ sudo rm -rf node_modules
